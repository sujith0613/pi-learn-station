# Python
.venv/
__pycache__/
*.pyc
*.log

# Node
node_modules/
web/dist/

# Training artifacts (large / regenerable)
data/raw/
ml/checkpoints/
ml/*.pt
ml/*.onnx

# uv
.uv/

# OS
.DS_Store

# Runtime models we DO commit: exported ONNX + json live in models/ for the Pi.
# PyTorch checkpoints (.pt) are large, regenerable, and unused at runtime.
models/**/*.pt
models/*.pt

# Large build/deploy artifacts (regenerate or re-save)
dist/
